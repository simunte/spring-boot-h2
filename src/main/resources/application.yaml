server:
  port: ${SERVER_PORT}

spring:
  datasource:
    url: jdbc:h2:file:/Users/toms/Documents/PERSONAL/GitHub/spring-boot-h2-datasource/datasource
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false